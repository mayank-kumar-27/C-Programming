#include<stdio.h>
int main()
{
    int n;
    printf("Enter the number upto which u want the numbers to be printed :");
    scanf("%d",&n);

    for(int i=1; i<=n; i++)
    
    printf("%d\n", i);
}